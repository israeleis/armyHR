import React from 'react'
import ReactDOM from 'react-dom/client'
import './styles/globals.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <div style={{ padding: 24, color: '#c3cc8c', fontFamily: 'Public Sans' }}>
      <h1>יומן מצבת כוח אדם</h1>
      <p>App scaffold complete. Implementation in progress.</p>
    </div>
  </React.StrictMode>,
)
